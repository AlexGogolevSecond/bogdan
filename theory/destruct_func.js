const userProfile = {
    name: 'Alex',
    commentsQnt: 25,
    hasLalala: true,
}

const userInfo = (profile) => {
    let name, commentsQnt, hasLalala = profile
    // console.log(`name: ${name}`);
    // console.log(`commentsQnt: ${commentsQnt}`);
    // console.log(`hasLalala: ${hasLalala}`);
    if (!commentsQnt) {
        return 
    }

}

userInfo(userProfile)

